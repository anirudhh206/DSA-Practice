#include <bits/stdc++.h>
using namespace std;

struct job
{
    int id, deadline, profit;
};

int main()
{
    int n;
    cin >> n;
    vector<job> job;
    for (int i = 0; i < n; i++)
    {
        cin >> job[i].id >> job.[i].deadline >> job[i].profit;
    }
}
